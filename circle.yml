machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t codius/codius.org .
test:
    override:
      - docker run -d -p 8080:8080 codius/codius.org; sleep 5
      - curl --retry 10 --retry-delay 5 -v http://localhost:8080
